import 'package:flutter/material.dart';

class ChatButtonAsk extends StatelessWidget {
  final int? userId;
  final String? lotStatus;
  final int lotId;
  final int own;
  final int? userId2;

  const ChatButtonAsk({
    Key? key,
    required this.userId,
    required this.userId2,
    required this.lotStatus,
    required this.lotId,
    required this.own,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {
        // Действия для ask‑лота
      },
      style: ElevatedButton.styleFrom(
        padding: const EdgeInsets.symmetric(vertical: 16),
        shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(30)),
      ),
      child: Text(
        'ASK - UserId: $userId, LotId: $lotId, Status: $lotStatus, Own: $own',
        textAlign: TextAlign.center,
      ),
    );
  }
}

займемся пока только этим виджетом его логика делиться на 2 части в зависимости от own 1 или 0

если own = 1: 
если lotStatus = 3 то кнопка не активна и в ней текст 'выбрать победителем'
если lotStatus = 4 то кнопка активна и в ней текст 'выбрать победителем' при нажатии отправить запрос на {BASE_API_URL}/api/choose_winner и передать userId, userId2, lotId
